[package]
name = "trawl"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
colored = "3.1.1"
indicatif = "0.18.6"

[profile.release]
debug = true # keeps symbol names/line info in the optimized binary for profilers like samply
